class Contact {
  id: string;
  name: string;
  birthDate: string;

  constructor(id: string, name: string, birthDate: string) {
    this.id = id;
    this.name = name;
    this.birthDate = birthDate;
  }
}
